// importing the necessary modules from the std library and also external crates
use std::collections::HashMap; //Used for storing the substitution key
use rand::seq::SliceRandom; //for shuflling the alphabet
use rand ::thread_rng; //Also for generating random numbers
use rand::Rng; //for generating random numbers
// function to generate a random substitution key
fn generate_key() -> HashMap< char, char> {

    // We need to create a vector
}